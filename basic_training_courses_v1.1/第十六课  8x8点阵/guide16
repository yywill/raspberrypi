现在开始学习我们的基础套件的十六课，树莓派直接驱动8x8点阵，使用动态扫描的方式驱动，同样和之前我们
动态扫描驱动四位数码管一样的，首先我们要知道8x8点阵原理，8行8列的LED灯组成8x8的点阵，行端接的是正
列端接的是负，结构图见图三，我们定义行的序号为01234567，列序号为ABCDEFGH，同样从正面看过去我们可
以看到点阵背后有两行的针脚。有黑色型号的标示符朝下，这样我们从左到右定义引脚，先从上面的一行开始
从左到右依次是HG1A3FD0，下面的一行的针脚从左到右则分别是46BC7E52。知道了这个引脚序号之后我们就可
以根据点阵的原理开始写程序驱动点阵了。
